1. transition 글 눌렀을 때 생기는 방법
2. action 에서 context.contactList.pageno가 객체의 값으로 들어가질 않는다.
{page.no : context.contactList.pageno} // 뒤의 pageno를 읽을 수 없다.

--------


1. noticeForm 에서 computed
그저 data 바꾸기만 하기 위한 용도일 때
return 값을 어떻게 해야하나

 + this.notice가 Unexpected side effect in "doneNoticeOne" .. 빨간줄이 뜨는 데
slice 해결법은 본인 자체가 되는게 아니라 새로운 걸 뱉어내는 것 아닌가 그럼 어떻게..
